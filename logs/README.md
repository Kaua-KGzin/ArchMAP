# Logs

This folder centralizes runtime and historical logs for local development.

## Structure

- `runtime/`: ephemeral local logs (ignored by git).
- `archive/`: curated historical logs that document important incidents.

## Notes

- Do not commit machine-specific runtime logs.
- Keep only logs that are useful for troubleshooting history.
